# Database
DATABASE_URL=postgresql://stackfolio:stackfolio@localhost:5432/stackfolio
POSTGRES_USER=stackfolio
POSTGRES_PASSWORD=stackfolio
POSTGRES_DB=stackfolio

# API
API_PORT=3001
WEB_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# Redis (optional — docker compose --profile with-redis up -d redis)
REDIS_URL=redis://localhost:6379

# Auth (Auth.js / NextAuth)
AUTH_SECRET=change-me-to-a-long-random-string
GITHUB_ID=
GITHUB_SECRET=
DEV_AUTH_EMAIL=dev@stackfolio.local
DEV_AUTH_PASSWORD=devpassword

# Default user for seed (matches dev credentials)
DEFAULT_USER_ID=dev-user-001
